<UserControl x:Class="ClipSpy.Views.FormatsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding TotalFormats, StringFormat='Total formats: {0}'}"
                   Foreground="{DynamicResource AccentLightBrush}"
                   FontSize="11" FontWeight="SemiBold" Margin="0,0,0,8" />

        <DataGrid Grid.Row="1" ItemsSource="{Binding Formats}" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdDisplay}" Width="120" />
                <DataGridTextColumn Header="Format Name" Binding="{Binding Name}" Width="220" />
                <DataGridTextColumn Header="Size (bytes)" Binding="{Binding SizeBytes}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="FontFamily" Value="{DynamicResource MonoFont}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="80" />
                <DataGridTextColumn Header="HGLOBAL Handle" Binding="{Binding HandleHex}" Width="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="{DynamicResource MonoFont}" />
                            <Setter Property="FontSize" Value="11" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridCheckBoxColumn Header="Standard" Binding="{Binding IsStandard, Mode=OneWay}" Width="70" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
