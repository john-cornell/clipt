<UserControl x:Class="Clipt.Views.TextHexTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Clipt.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="200" />
        </Grid.ColumnDefinitions>

        <views:TextTabView x:Name="TextPane"
                           Grid.Column="0"
                           DataContext="{Binding TextTab}" />

        <GridSplitter Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource BorderBrush}"
                      ResizeBehavior="PreviousAndNext" />

        <views:HexTabView x:Name="HexPane"
                          Grid.Column="2"
                          DataContext="{Binding HexTab}" />
    </Grid>
</UserControl>
