<UserControl x:Class="Clipt.Views.HexTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             xmlns:converters="clr-namespace:Clipt.Converters"
             xmlns:help="clr-namespace:Clipt.Help">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:InverseBoolConverter x:Key="InverseBool" />
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <WrapPanel Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Format:" Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="11.5" VerticalAlignment="Center" Margin="0,0,8,0"
                       ToolTip="{x:Static help:HelpContent.HexDumpTooltip}" />
            <ComboBox ItemsSource="{Binding AvailableFormats}"
                      SelectedItem="{Binding SelectedFormat}"
                      MinWidth="260">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" ToolTip="{Binding Description}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="Bytes/Row:" Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="11.5" VerticalAlignment="Center" Margin="20,0,8,0"
                       ToolTip="{x:Static help:HelpContent.BytesPerRowTooltip}" />
            <ComboBox SelectedItem="{Binding BytesPerRow}" MinWidth="70">
                <sys:Int32>8</sys:Int32>
                <sys:Int32>16</sys:Int32>
                <sys:Int32>32</sys:Int32>
            </ComboBox>

            <TextBlock Text="{Binding TotalBytes, StringFormat='Total: {0} bytes'}"
                       Foreground="{DynamicResource AccentSecondaryBrush}"
                       FontSize="11.5" FontWeight="SemiBold" VerticalAlignment="Center" Margin="20,0,0,0" />
        </WrapPanel>

        <!-- Format info -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundDarkBrush}"
                BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1"
                CornerRadius="8"
                Padding="12,8" Margin="0,0,0,10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedFormat}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource AccentSecondaryBrush}"
                           Text="{Binding SelectedFormat.Name, StringFormat='About: {0}'}"
                           Margin="0,0,0,4" />
                <TextBlock Text="{Binding SelectedFormatDetailedDescription}"
                           TextWrapping="Wrap" FontSize="11.5"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
            </DockPanel>
        </Border>

        <!-- Edit toolbar -->
        <DockPanel Grid.Row="2" Margin="0,0,0,8">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Style="{StaticResource PrimaryButtonStyle}"
                        Content="Apply to Clipboard" Command="{Binding ApplyHexToClipboardCommand}"
                        Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                        Margin="0,0,8,0" Padding="12,5" FontSize="11.5" />
                <Button Command="{Binding ToggleEditingCommand}" Padding="12,5" FontSize="11.5">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Content" Value="Edit" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter Property="Content" Value="Cancel" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
            <TextBlock Text="{Binding EditStatusMessage}"
                       Foreground="{DynamicResource AccentSecondaryBrush}" FontSize="11.5"
                       VerticalAlignment="Center" />
        </DockPanel>

        <!-- Hex dump: offset | hex | ASCII -->
        <Border Grid.Row="3" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1"
                Background="{DynamicResource CardBackgroundDarkBrush}"
                CornerRadius="8" ClipToBounds="True">
            <ScrollViewer x:Name="HexScrollViewer"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Offset column -->
                    <TextBlock x:Name="OffsetTextBlock"
                               Grid.Column="0"
                               Text="{Binding OffsetColumn}"
                               FontFamily="{DynamicResource MonoFont}" FontSize="12"
                               Foreground="{DynamicResource TextMutedBrush}"
                               Padding="10,6,10,6" />

                    <!-- Hex column -->
                    <TextBox x:Name="HexTextBox"
                             Grid.Column="1"
                             Text="{Binding HexColumn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             FontFamily="{DynamicResource MonoFont}" FontSize="12"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             Background="Transparent"
                             BorderThickness="1,0,1,0"
                             BorderBrush="{DynamicResource BorderSubtleBrush}"
                             Padding="6,6"
                             IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBool}}"
                             IsInactiveSelectionHighlightEnabled="True"
                             TextWrapping="NoWrap"
                             AcceptsReturn="True"
                             HorizontalScrollBarVisibility="Disabled"
                             VerticalScrollBarVisibility="Disabled" />

                    <!-- ASCII column -->
                    <TextBox x:Name="AsciiTextBox"
                             Grid.Column="2"
                             Text="{Binding AsciiColumn, Mode=OneWay}"
                             FontFamily="{DynamicResource MonoFont}" FontSize="12"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="6,6"
                             IsReadOnly="True"
                             IsInactiveSelectionHighlightEnabled="True"
                             TextWrapping="NoWrap"
                             AcceptsReturn="True"
                             HorizontalScrollBarVisibility="Disabled"
                             VerticalScrollBarVisibility="Disabled" />
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
